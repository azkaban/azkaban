apply plugin: "maven-publish"

publishing {
    repositories {
        maven {
            url findProperty("nexusThirdPartyUrl")
            credentials {
                username findProperty("nexusUsername")
                password findProperty("nexusPassword")
            }
        }
    }
}
